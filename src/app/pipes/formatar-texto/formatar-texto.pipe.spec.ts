import { FormatarTextoPipe } from './formatar-texto.pipe';

describe('FormatarTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatarTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
